const server = require('../index.js');
const config = require('../config');

const _port = (config.WWW_PORT || 3000);
server.listen(_port, () => {
    console.log(` Server is listening on ${_port} `);
})